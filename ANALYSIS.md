# 인지 · 계획 · 제어 비교 분석 — GR00T N1.7 vs OpenVLA-OFT

LIBERO-Spatial, task 0, 동일 관측 5프레임 재생.
성공률은 GR00T 98.0% / OpenVLA-OFT 100% 로 사실상 동률.
그러나 **같은 관측에 대한 내부 반응은 전혀 다르다.**

---

## 0. 결론 먼저

| # | 발견 | 근거 수치 |
|---|---|---|
| 1 | **GR00T 는 비결정적, OpenVLA-OFT 는 완전 결정적** | original 0.085 vs **0.000** |
| 2 | GR00T 의 언어 반응은 **자기 샘플링 노이즈에 묻혀 있다** | target_swap 0.096 ≈ 노이즈 0.085 |
| 3 | OpenVLA-OFT 는 **wrist 카메라에 7배 편중** | agentview 0.218 / wrist 1.526 |
| 4 | 유일하게 정책 중립적인 프레임(0번)에서 **가장 크게 갈린다** | ‖Δ‖ = 2.143, 그리퍼 지령이 정반대 |

이 넷은 전부 **아키텍처 차이로 설명된다.** 우연이 아니다.

---

## 1. 두 모델은 애초에 다른 종류의 물건이다

| | GR00T N1.7 | OpenVLA-OFT |
|---|---|---|
| 계열 | 이중 시스템 (VLM + DiT) | 단일 LLM + 회귀 헤드 |
| 백본 | Eagle-2.5 VLM (**동결**) | Llama-2 7B + Prismatic(DINOv2+SigLIP) |
| 액션 생성 | **flow matching** (DiT 반복 디노이징) | **L1 회귀** (MLP-ResNet 1회 forward) |
| 언어 경로 | 동결 VLM → cross-attention → DiT | 언어 토큰이 **LLM 시퀀스에 직접** 들어감 |
| 결정성 | **비결정적** (가우시안 노이즈 샘플링) | **결정적** |
| 청크 | 16 | 8 |
| 파라미터 | 3B (VLM 1.34B 수준) | 7B |

GR00T N1 계열은 flow matching 으로 액션을 생성하며, DiT 가 로봇의 고유수용 상태와
액션을 처리하고 Eagle VLM 의 이미지·텍스트 토큰과 cross-attention 하여
디노이즈된 모터 액션을 출력한다.
추론 시 정책은 **먼저 가우시안 노이즈 벡터를 샘플링한 뒤**
속도 예측을 이용해 연속값 액션을 반복적으로 재구성한다.

OpenVLA-OFT 는 병렬 디코딩(양방향 어텐션), 액션 청킹, 연속 액션 표현,
단순 L1 회귀 목적함수의 조합이다. 이산 액션 토큰 출력을 **MLP 회귀 헤드**로 대체했고,
이 헤드는 액션 쿼리 토큰 위치의 최종층 hidden state 를 받아 연속 액션 청크로 직접 매핑한다.
LIBERO 평균 성공률을 76.5% → 97.1% 로 올렸고 액션 생성 처리량은 26배 빨라졌다.

**N1.5 부터 VLM 은 사전학습·파인튜닝 양쪽에서 동결된다.**
언어 이해를 보존하고 일반화를 개선하기 위한 설계다.
→ **이 한 줄이 발견 2를 거의 다 설명한다. 뒤에서 다시 본다.**

---

## 2. 발견 1 — 결정성: 0.085 vs 0.000

```
                original   synonym  target_swap   empty  irrelevant
GR00T N1.7        0.085      0.094      0.096      0.907    0.921
OpenVLA-OFT       0.000      0.031      0.172      0.240    0.233
```

`original` 은 **같은 이미지에 같은 프롬프트를 두 번 넣은 것**이다.
정의상 0이어야 한다. OpenVLA-OFT 는 정확히 0.000 이다.
**GR00T 는 0.085 다.**

### 왜

flow matching 추론은 가우시안 노이즈 벡터를 샘플링하는 것으로 시작한다.
시드를 고정하지 않으면 매 호출마다 다른 노이즈에서 출발하므로 **출력이 달라진다.**
L1 회귀 헤드는 hidden state → 액션의 결정적 함수이므로 같은 입력에 항상 같은 출력을 낸다.

### 이게 왜 중요한가

**0.085 는 GR00T 의 노이즈 바닥(noise floor)이다.**
이보다 작은 변화는 측정 불가능하다. 그런데:

```
synonym      0.094  →  노이즈 바닥 대비 +0.009
target_swap  0.096  →  노이즈 바닥 대비 +0.011
```

**GR00T 가 "black bowl" 을 "plate" 로 바꾼 지시에 보이는 반응은
자기 자신을 두 번 호출했을 때의 흔들림과 구분되지 않는다.**

OpenVLA-OFT 는 노이즈 바닥이 0이므로 0.172 가 그대로 신호다.
synonym(0.031) 의 5.5배 → **의미 변화를 실제로 감지한다.**

### 재계산: 노이즈를 뺀 언어 민감도

```
                 (target_swap - original) / (empty - original)
GR00T N1.7        (0.096-0.085)/(0.907-0.085) = 0.011/0.822 =  1.3 %
OpenVLA-OFT       (0.172-0.000)/(0.240-0.000) = 0.172/0.240 = 71.7 %
```

단순 비율(11% vs 72%)보다 **훨씬 극단적인 격차**가 드러난다.

> **주의.** 0.085 는 표본 1개다. 노이즈 바닥을 제대로 추정하려면 같은 입력을
> N회(≥20) 반복해 분포를 봐야 한다. 이건 §7의 첫 번째 후속 실험이다.

---

## 3. 발견 2 — 언어: 동결된 VLM 의 대가

GR00T 는 "지시문이 **있는가**" 는 확실히 본다 (empty 0.907, irrelevant 0.921 — 노이즈의 10배).
그러나 "지시문이 **무엇을 말하는가**" 는 거의 안 본다 (target_swap +0.011).

### 아키텍처가 이걸 예측한다

**GR00T**: 언어 → 동결된 Eagle-2.5 VLM → 임베딩 → DiT 가 cross-attention.
LIBERO 후속학습에서 **VLM 은 갱신되지 않는다.** 학습되는 건 DiT 와 어댑터뿐이다.
DiT 입장에서 텍스트는 "cross-attention 으로 들어오는 조건 벡터 하나" 이고,
LIBERO-Spatial 의 10개 태스크 지시문이 서로 거의 같으면
**그 조건 벡터를 세밀하게 구분할 유인이 학습 과정에 없다.**
텍스트가 있냐 없냐(분포 밖 입력이냐)만 구분하면 손실이 충분히 줄어든다.

**OpenVLA-OFT**: 언어 토큰이 Llama-2 시퀀스에 직접 들어가고,
액션 헤드는 그 시퀀스의 최종층 hidden state 를 읽는다.
LoRA 가 self-attention 과 MLP 의 모든 선형층에 붙어 **LLM 자체가 파인튜닝된다.**
언어와 액션 사이에 동결 장벽이 없다.

### 확인해야 할 반론

OpenVLA-OFT 의 LIBERO 공식 체크포인트는 **`use_film = False`** 다.
FiLM 은 언어에 기반해 시각 특징에 학습된 아핀 변환을 적용하여
태스크별 언어 단서에 따라 시각 표현을 조절하는 언어 그라운딩 강화 장치인데,
LIBERO 체크포인트에는 꺼져 있다.

즉 **OpenVLA-OFT 는 언어 그라운딩 보조 장치 없이도 GR00T 보다 언어를 더 본다.**
이건 FiLM 유무가 아니라 **동결 vs 파인튜닝** 이라는 더 근본적인 차이 때문이라는 해석을 지지한다.

### 단정하면 안 되는 이유

LIBERO-Spatial 의 10개 태스크는 전부
`"pick up the black bowl <위치 수식어> and place it on the plate"` 변형이다.
**목적어가 항상 같다.** 구분해야 하는 건 위치 수식어뿐이다.
이 벤치마크에서는 "무엇을" 이 아니라 "어디에 있는" 만 중요하므로,
GR00T 가 목적어를 무시하도록 학습되는 것이 이 데이터에서는 **합리적 최적해**다.

→ `libero_object` (목적어가 바뀜) 와 `libero_goal` (목표가 바뀜) 에서
   같은 패턴이 반복되는지 확인해야 주장이 성립한다. §7 참조.

---

## 4. 발견 3 — 카메라: wrist 편중

```
              agentview   wrist
GR00T N1.7      1.232     1.224    ← 균형
OpenVLA-OFT     0.218     1.526    ← wrist 7.0배
```

OpenVLA-OFT 는 3인칭 뷰 이미지에 더해 **wrist 뷰 이미지와 로봇의 고유수용 상태**를
언어 디코더에 함께 제공한다. LIBERO 설정은 `num_images_in_input=2`, `use_proprio=True` 다.

각 이미지는 융합 비전 인코더를 거쳐 **256개 패치 임베딩**을 만들고
3층 MLP 로 언어 임베딩 공간에 투영된다. 저차원 로봇 상태도 2층 MLP 로 투영된다.

### 해석

OpenVLA-OFT 는 **wrist 뷰 + proprio 조합으로 agentview 를 대체 가능**하게 학습된 것으로 보인다.
- proprio(8차원 EEF 자세)가 "팔이 어디 있는가" 를 이미 알려준다
- wrist 뷰가 "집을 물체가 그리퍼 앞 어디에 있는가" 를 알려준다
- → 3인칭 뷰의 공간 정보가 **중복**된다

GR00T 는 두 뷰가 동등하게 중요하다. DiT 가 cross-attention 으로 VLM 토큰을 참조하는 구조에서
두 카메라가 같은 VLM 을 거쳐 하나의 토큰 시퀀스로 합쳐지므로,
어느 한쪽을 지우면 시퀀스 전체가 교란된다.

### 실무적 함의

**OpenVLA-OFT 는 3인칭 카메라가 가려지거나 흔들려도 버틸 가능성이 높다.**
반대로 **wrist 카메라가 오염되면 즉시 무너진다.**
GR00T 는 어느 쪽이든 절반의 타격을 받는다.

실제 로봇에서 wrist 카메라는 물체에 가려지거나 조명이 급변하기 쉬운 위치다.
이 편중은 배포 환경에 따라 장점도 단점도 된다.

> **단, 청크 길이 보정이 필요하다.** ‖Δ‖ 는 (16,7) 과 (8,7) 위에서 계산됐다.
> 원소별 차이가 같다면 GR00T 쪽이 √2 ≈ 1.41배 커진다.
> GR00T 1.232 ÷ 1.41 ≈ 0.87 로 보정해도 **두 카메라 균형이라는 결론은 유지**된다.
> 그러나 모델 간 절대값 비교는 이 보정 없이는 부당하다. §6 참조.

---

## 5. 발견 4 — 프레임 0 에서 가장 크게 갈린다

```
frame 0  ‖Δ first action‖ = 2.1433   ← 최대
frame 1                     0.0780
frame 2                     0.1134
frame 3                     0.1001
frame 4                     0.0835
```

프레임 0 은 `set_init_state` 직후의 리셋 상태다.
**어느 정책이 굴렸든 동일한 상태**이므로 이 프로젝트에서 **유일하게 정책 중립적인 프레임**이다.
나머지 1~4 번은 GR00T 가 만든 궤적 위의 상태라 OpenVLA 에게는 분포 밖이다.

그런데 **가장 공정한 프레임에서 불일치가 25배 크다.**

```
frame 0    GR00T:   dx 0.07  dy 0.02  dz 0.08   gripper OPEN  (-1)
           OpenVLA: dx 0.46  dy 0.59  dz 0.42   gripper CLOSE (+0.59)
```

GR00T 는 거의 움직이지 않고 그리퍼를 연다.
OpenVLA 는 큰 폭으로 움직이며 **그리퍼를 닫는다.**

### 해석 가설 (검증 필요)

1. **초기 상태 처리 차이.** GR00T 는 첫 스텝을 "관측을 확인하는 스텝" 으로 쓰고
   OpenVLA 는 즉시 궤적을 커밋한다. 청크 16 vs 8 과도 맞물린다 —
   지평이 길수록 첫 스텝을 보수적으로 쓸 여유가 있다.
2. **그리퍼 규약 불일치 가능성.** 둘 다 LIBERO 파인튜닝이므로 규약은 같아야 하지만,
   프레임 0 에서 정반대 지령이 나온 것은 확인이 필요하다.
   `dataset_statistics.json` 의 그리퍼 정규화 통계를 대조할 것.
3. **리셋 직후 상태가 학습 분포에서 드물다.** 데모 수집 시 리셋 직후 몇 스텝이
   잘려나갔을 수 있다(`no_noops` 필터). 그렇다면 두 모델 모두 이 프레임에서
   분포 밖이고, 서로 다른 방향으로 외삽한 것이다.

**가설 2가 사실이면 이 프로젝트의 다른 결론도 재검토해야 한다.** 우선순위 최상.

---

## 6. 반드시 통제해야 할 교란 변수

이 분석을 공개하기 전에 아래를 해결하지 않으면 반박당한다.

### (a) 청크 길이 — 즉시 수정 가능

GR00T 16, OpenVLA 8. ‖·‖ 를 서로 다른 크기의 텐서 위에서 계산했다.
→ **스텝당 RMS 로 정규화**해야 한다.

```python
d = np.linalg.norm(A - B) / np.sqrt(A.size)     # 원소당 RMS
# 또는 공통 지평만 비교
d = np.linalg.norm(A[:8] - B[:8]) / np.sqrt(8 * 7)
```

§2 의 **비율 계산은 각 모델 내부 비교**이므로 이 문제의 영향을 받지 않는다.
모델 간 절대값 비교(§4 카메라)만 보정이 필요하다.

### (b) off-policy 재생

프레임 1~4 는 GR00T 의 궤적이다. OpenVLA 는 자기 정책으로는 가지 않았을 상태를 본다.
→ 대칭을 맞추려면 **OpenVLA 로도 에피소드를 덤프**해서 교차 평가해야 한다.

```
             재생 대상 →   GR00T 궤적   OpenVLA 궤적
평가 모델 ↓
GR00T                        on-policy    off-policy
OpenVLA                      off-policy   on-policy
```

대각선과 비대각선의 차이가 곧 **분포 이동에 대한 강건성**이다. 그 자체로 좋은 결과다.

### (c) 표본 1개

노이즈 바닥 0.085, 카메라 비율, 언어 민감도 전부 **프레임 1개 · 태스크 1개 · 시드 1개**다.
일반화 주장 금지.

### (d) jerk 플롯은 지금 의미가 없다

현재 jerk 는 **프레임 간** 차분이라 관측 변화를 반영할 뿐 모델의 매끄러움이 아니다.
프레임이 5개뿐이라 점이 3개다.
→ **청크 내부** 차분으로 바꿔야 "이 모델이 덜컥거리는가" 를 잰다.

```python
jerk = np.linalg.norm(np.diff(chunk, n=2, axis=0), axis=1)   # 청크 안에서
```

---

## 7. 다음 실험 — 우선순위 순

### ① 노이즈 바닥 측정 (30분, 가장 중요)
같은 관측·같은 프롬프트로 **N=30회** 추론.
- GR00T: 분포의 평균·표준편차 → 신뢰구간
- OpenVLA: 전부 0 이어야 함 (아니면 어딘가 잘못됨)
- **그 뒤 모든 언어/카메라 수치를 "노이즈 바닥의 몇 배" 로 재보고**

이거 없이는 §2, §3 의 주장이 성립하지 않는다.

### ② 그리퍼 규약 확인 (10분)
각 체크포인트의 `dataset_statistics.json` 에서 7번째 차원의 정규화 통계 대조.
프레임 0 의 정반대 지령이 규약 문제인지 판단.

### ③ suite 확장 (2시간)
`libero_object` (목적어가 실제로 바뀜), `libero_goal` (목표가 바뀜) 에서 §3 반복.
- **object 에서도 GR00T 의 target_swap 이 노이즈에 묻히면** → 진짜 언어 무시
- **object 에서는 반응하면** → spatial 벤치마크 특성에 최적화된 것 (덜 자극적이지만 더 정확한 결론)

### ④ 교차 재생 (1시간)
§6(b) 의 2×2. off-policy 강건성.

### ⑤ 섭동 실험 (반나절)
물체 초기 위치에 노이즈를 주고 성능 저하 곡선.
선행 연구는 표준 LIBERO 가 훈련 태스크와 동일한 태스크를 평가하며
차이는 물체 초기 상태의 미세한 섭동뿐이라고 지적한다.
여기에 실제 섭동을 주면 97~98% 클러스터가 갈라진다.

---

## 8. 지금 시점의 정직한 요약

> LIBERO-Spatial 에서 GR00T N1.7(98.0%)과 OpenVLA-OFT(100%)는 성공률로 구분되지 않는다.
> 그러나 동일 관측을 재생해 내부 반응을 비교하면 세 가지가 갈린다.
>
> **첫째, 결정성.** OpenVLA-OFT 의 L1 회귀 헤드는 같은 입력에 항상 같은 액션을 낸다(Δ=0.000).
> GR00T 의 flow-matching 헤드는 매 호출마다 가우시안 노이즈를 새로 뽑으므로
> 같은 입력에도 Δ=0.085 만큼 흔들린다. 이 값이 GR00T 의 측정 바닥이다.
>
> **둘째, 언어.** 노이즈 바닥을 보정하면, 대상 물체를 바꾼 지시에 대한 반응이
> GR00T 는 1.3%, OpenVLA-OFT 는 71.7% 다. GR00T 는 지시문의 유무는 감지하지만
> 내용은 거의 반영하지 않는다. GR00T N1.5+ 가 VLM 을 동결한 채
> DiT 만 후속학습하는 구조와, LIBERO-Spatial 의 지시문이 목적어를 공유한다는
> 데이터 특성이 함께 작용한 결과로 보인다.
>
> **셋째, 시각 의존.** OpenVLA-OFT 는 wrist 카메라에 7배 편중돼 있고
> GR00T 는 두 뷰를 균형 있게 쓴다. OpenVLA-OFT 가 proprio 를 함께 받는다는 점에서,
> wrist+proprio 가 3인칭 뷰를 상당 부분 대체한 것으로 해석된다.
>
> **단, 이 결과는 태스크 1개·프레임 1개·시드 1개이며 청크 길이(16 vs 8) 보정 전이다.
> §7 의 ①②③ 을 마치기 전까지는 가설이다.**

이 마지막 문단을 README 에서 빼지 말 것. 있는 편이 훨씬 신뢰를 준다.
